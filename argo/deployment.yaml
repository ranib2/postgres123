apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: webapp
spec:
  template:
  selector:
      matchLabels:
        app.kubernetes.io/name: sawtooth-0
    metadata:
      labels:
        app: webapp
    spec:
      containers:
      - image: alpine:latest
        name: go-url-shortener
        
